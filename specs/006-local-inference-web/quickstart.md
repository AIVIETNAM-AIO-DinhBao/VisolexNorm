# Nghiệm thu nhanh Phase 6

```bash
pip install -r requirements.txt
python -m app.inference --text "mik ko bt hnay đi hc ko"
python -m app.web
```

Mở URL local Gradio, thử input rỗng, câu chuẩn, teencode, viết tắt và nhiều lần liên tiếp.
Tắt mạng sau khi model đã có cache/local và xác nhận app vẫn normalize được.